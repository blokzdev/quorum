# Backlog — out-of-scope work, captured then deferred

> Append-only, one line per item. **Two capture sources** (CLAUDE.md → Operating doctrine): *reactive* —
> work fails the scope-wall four-checks mid-implementation; and *generative* — the adversarial-validation
> / design fan-out surfaces a vision-aligned adjacency beyond scope (harvest it, don't discard it). Either
> way: append a line and keep moving — capture must be cheaper than doing it; **capture ≠ commit** (acting
> still pays the full four-check wall). Do NOT design, prioritize, or groom mid-phase; **drain at phase
> close-out** (triage into the next phase plan or close as won't-do).
>
> **Routing** — this file is for *homeless enhancements* only. A coherent future-phase **feature/capability**
> goes to [`roadmap.md`](roadmap.md)'s phase band, NOT here. The vision-aligned bar gates entry (advances a
> stated bet or named phase, else drop it for real). A `security` / `correctness` / `data-loss` item does
> NOT belong here — it goes straight to `HUMAN.md` the same session.
>
> Format: `- [YYYY-MM-DD] (subphase) <what> — <enhancement | future-subphase | net-new-scope> — <S/M/L>`

- [2026-06-27] (P2.3 C6) Make the custom Model Studio controls (buttons/chips/depth toggles + nav tabs) keyboard-operable — enhancement (a11y, review LOW) — M
- [2026-06-27] (P2.3 C6) Filled-button WCAG AA contrast (white-on-accent 3.77:1) — darken fill / add an onAccent token; brand decision — enhancement — S
- [2026-06-27] (P2.4) Run Comparison: diff two runs of the same ticker across model configs/dates (P2.4d stretch) — future-subphase — M
- [2026-06-27] (P2.5) Per-role *effort* UI control in Model Studio (the wire seam `spec.effort` already ships dormant) — enhancement — M
- [2026-06-27] (MO) Optional CI/pre-commit check: fail if HUMAN.md "Last AI update" is older than the newest commit touching apps/ or tradingagents/ — enhancement (HITL anti-rot insurance) — S
- [2026-06-27] (Phase 3; deferred post-V1 by founder 2026-07-06) Rotate the shared Gemini test key — it's a **dev/CI-only** credential (gitignored, never ships, separate from the product's per-run keychain BYOK), so this is dev-hygiene not a GA gate; Phase 4 keeps using it and the secret-scan CI gate (P4.1a) is the GA-relevant protection — enhancement (dev hygiene) — S
- [2026-06-27] (P2.5c1) Per-role OpenAI-compatible / Ollama **base-URL field** so a role can pin a custom/local endpoint independent of the global provider (c1 excludes openai_compatible per role to avoid a broken run) — future-subphase (P2.5c2+) — M
- [2026-06-27] (P2.5c1) Non-destructive **"fill only unassigned"** apply-to-all variant (and/or confirm-before-clobber); c1 ships the simpler destructive overwrite — enhancement — S
- [2026-06-27] (P2.5c1) Bench row summary should show **"N roles"** so users see that applying a Bench REPLACES (not merges) the whole Dream Team lineup — enhancement — S
- [2026-06-27] (P2.5c1) **Live-terminal cast list** — surface the resolved roster on the verdict rail the moment a run completes (needs the resolved map plumbed into RunViewState); advances the debate-terminal transparency bet — future-subphase — M
- [2026-07-04] (P2.6c) Flutter version bump is a **coupled goldens re-baseline**: CI pins the exact Flutter 3.38.6 the 8 goldens were byte-exact rasterized on, so bumping Flutter/Dart requires regenerating + Read-verifying all goldens in the same PR (byte-exact by design). Note this on any SDK bump — enhancement (process) — S
- [2026-07-04] (P2.6c) Verify `packaging.yml` end-to-end via one `workflow_dispatch` run once it reaches `main` (workflow_dispatch needs the file on the default branch) — build script + deps confirmed sound, but the CI run (choco Inno, runner CRT/signtool, PyInstaller on the hosted image) is unproven — future-subphase (Phase 3 release CI) — S
- [2026-07-04] (P2.6b) Bedrock in the frozen installer: `langchain_aws` + `boto3` aren't repo deps, so `bedrock_client` is intentionally not bundled — a Bedrock run through the installed app fails. Add the deps + hiddenimports (and validate) if Bedrock support is wanted — future-subphase — M
- [2026-07-04] (P2.6b) Add a **provider-freeze regression test**: a headless real run per provider family (openai-compat + one native cloud) that asserts a non-empty report — the demo contract check can't catch a missing provider package (the P2.6b HIGH slipped past a demo-only "11/11") — enhancement (CI, needs a runner with Ollama/a key) — M
- [2026-07-04] (P2.6a) Harden the stale-reap PID check: `_isLiveSidecar` scans `tasklist` stdout for `contains('$pid')`, which could match a PID substring elsewhere in the row (e.g. a memory figure) — parse the row/CSV properly. Pre-existing (not introduced by the spawn-path change); low risk (guarded by the `/FI IMAGENAME` filter) — enhancement — S
- [2026-06-27] (P2.5c1) **Serve the Dream Team roster via `/catalog`** (ROLE_KEYS + labels + DEEP_ROLES + stages) so the desktop stops hand-mirroring agent_roles.py — kills a whole class of drift (Model Studio + cast list); a contract/endpoint change for a future Model Studio hardening — future-subphase — M
- [2026-06-27] (P2.5c2) **Launch-time capability backstop** — re-check the EFFECTIVE tool-role model at launch (incl. the global quick model that runs unassigned tool roles, and bench/apply-introduced combos) and gate the run, not just the picker. Out of c2 (a new mechanism beyond "assigning… blocked in the UI"); moot today (denylist empty) but real once the denylist grows — future-subphase — M
- [2026-06-27] (P2.5c2) **Surface the `_NON_TOOL_MODELS` denylist** (or a capability column) in `model_catalog` so the block path is exercised by real catalog data, not only injected test flags — enhancement — S
- [2026-06-27] (P2.5c2) **Key-gate "Set in Settings" deep-link** — make the Hub key-gate notice navigate to the Settings provider/key field (c2 ships text-only to avoid nav plumbing) — enhancement — S
- [2026-07-05] (P3.1) **Comma-chain vendor fallback UI** — the engine's `route_to_vendor` already supports a comma-separated vendor chain per category (primary,fallback for data resilience); P3.1 exposes only a single vendor per category. Surface the fallback chain in the Data-sources picker — enhancement (data resilience) — M
- [2026-07-05] (P3.1) **Tool-level vendor override** — the engine's `get_vendor(category, method)` supports per-METHOD vendor config (finer than per-category); P3.1's `data_vendors` is per-category only. Expose per-tool overrides for power users — future-subphase — M
- [2026-07-05] (P3.1) **Vendor-tagged run provenance** — record which vendor actually served each category in the run manifest, so a Track Record scorecard can attribute a verdict to its data sources ("used Alpha Vantage fundamentals"). Advances the Track Record bet — future-subphase (Track Record) — S
- [2026-07-05] (P3.1) **Optional-category OFF switch** — genuinely disabling an optional category (macro/prediction_markets) needs an engine change: today empty→defaults and a bogus vendor→hard-raise, so "off" is inexpressible. P3.1 dropped the misleading on/off toggles for a FRED-key-enables-macro model. A real off-switch (category-level skip in `route_to_vendor`) is an engine capability — future-subphase — M
- [2026-07-05] (P3.1c review) **Catalog-driven vendor gate/merge** — `requiredVendorKeys`/`referencedVendorKeys` hardcode assumptions (core defaults keyless; `fred` is the sole always-merge optional keyed vendor). Sound today but won't generalize: if the engine ever ships a KEYED core default or a 2nd optional keyed vendor, the launch gate + key-merge should derive from the VendorCatalog's effective/optional-keyed vendors, not literals. Guarded by an INVARIANT comment in settings_controller.dart for now — enhancement (robustness) — S
- [2026-07-05] (P3.2b review) **Stale dropped-model displays as "Default" but still launches** — the `_Dropdown` coerces an out-of-list `value` to null for DISPLAY only (never rewrites `s.quickModel`); if discovery drops a previously-selected static Ollama id from the folded list, the picker shows "Default" while `buildLaunchConfig` still sends the stale id. Backstop classifies it null → no false-block, so it's a display/state divergence, not a gate failure. Pre-existing `_Dropdown` pattern (also on catalog changes) — rewrite the selection to null when it leaves the option set — enhancement (UX) — S
- [2026-07-05] (P3.3 review) **Demo path should showcase debate depth** — demo.py emits the old two-column bull/bear blobs + no structured recommendation, so demo mode (the cost-free QA path + first-user experience) shows the pre-P3.3 flat debate (turn thread falls back, balance bar keyword-scores). Emit ev.debate_turn(...) per synthetic turn + a structured `recommendation` on the investment_plan section; regenerate the demo_run.jsonl reducer fixture. Both fallbacks are correct, so this is showcase/verification polish, not a bug — enhancement — M
- [2026-07-05] (P3.3 review) **Cached reviews lose the structured recommendation** — hub_surface.cachedRunState rebuilds sections as ReportSection(k, v, null), so a re-opened finished run's balance bar downgrades from the structured 5-tier lean to prose keyword-scoring (the two can disagree). Thread the persisted /reports structured payload through the reconstruction if available — enhancement (fidelity) — S
- [2026-07-05] (Phase 3 close-out DRAIN) Backlog drained at phase-end: **vendor-tagged run provenance** (the P3.1 item above) → routed to `docs/roadmap.md` Band C / P7 Track Record (advances signature bet #1). The P3.1 vendor comma-chain fallback / tool-level override / optional-category OFF-switch stay here as **P3.1c2+ stretch** (subphase refinements, not new phases). The remaining LOW items (catalog-driven gate, stale-dropped-model display, demo depth showcase, cached-review structured fidelity) stay as enhancements. No security/correctness item left homeless (P3.5b look-ahead leak was fixed in-session).
- [2026-07-05] (P3 slice-verify) **Gate intra-integration sub-PR merges on the FULL flutter CI job** — P3.3/P3.4 sub-PRs self-merged while flutter was RED: `flutter analyze` (fatal-info on a P3.2b `_ollamaCatalog` lint) aborted BEFORE the test step, so their golden/widget tests never ran on CI at merge time; only the `phase-3 → main` run caught it (fixed forward in `15c025c`). Make the full flutter job (analyze+test+goldens+build) a required gate on merges into integration branches, not just → main, so a slice can't merge red again — future-subphase (Phase 4 release CI / hardening) — M
- [2026-07-05] (P3.3 slice-verify) **True separate risk-manager verdict node** — the engine sets `risk_debate_state.judge_decision = final_trade_decision` (portfolio_manager.py:75), so the RISK VERDICT ribbon necessarily shows the PM decision (the P3.3 "ribbon == risk judge, not PM decision" criterion was thus literally unsatisfiable; the ribbon is correct today). A distinct risk-manager node emitting its OWN verdict (separate from the PM's) would let the ribbon show an independent risk view — advances the debate-terminal depth bet (#3) / roadmap P12 (add analysts). Engine change — future-subphase (advanced AI / debate depth) — M

<!-- Phase-4 recon UI/UX/a11y audit (2026-07-06): 16 DEFER findings. 4 KEEP → P4.4 exit criteria (not here). 2 REJECT (hub-02 as-of caveat weight, hub-03 Hub disclaimer). Drain at Phase-4 close-out. -->
- [2026-07-06] (P4-recon hub-01) Launch-card config line renders provider as bare lowercase "ollama" (no Title-case, no model) vs Title-cased cloud providers — `_providerShort` has no ollama case + shows no model when deepModel is null — enhancement — S
- [2026-07-06] (P4-recon set-03) Danger "Clear" key button uses a neutral grey border not a danger-tinted one (`_SmallButton` ignores the `danger` flag for the border) — inconsistent with the accent-bordered Save — enhancement — S
- [2026-07-06] (P4-recon dt-01) Capability-BLOCKED role reads at near-equal weight to a WARN role (small recolored chip only; row not dimmed/disabled) — undersells the hard-block on bet #2's marquee gate — enhancement (bet #2) — S
- [2026-07-06] (P4-recon dt-02) Block/warn reason text shows only on an EXPANDED row; a collapsed blocked row gives no readable why (chip echoes a truncatable model id) — surface a terse inline reason ("No tool support") — enhancement (bet #2) — S
- [2026-07-06] (P4-recon dt-03) No golden covers the EXPANDED role state (12 open provider+model pickers, the disabled "no tools" dropdown item, inline capability notice, custom-id field) — add one expanded/blocked-row golden — enhancement (test coverage) — S
- [2026-07-06] (P4-recon term-01) Verdict rail is a near-empty ~700px void during the running debate (no cost card mid-debate) — the verdict-first IA's answer column sits blank through most of a live run; add a live-verdict/progress affordance — enhancement (bet #3) — M
- [2026-07-06] (P4-recon term-02) Mid-run the same agent (bear) renders in two places at once — full live card up top + "Building its case…" placeholder column below — reads as redundancy not liveness — enhancement (bet #3) — S
- [2026-07-06] (P4-recon term-03) Error state still paints the pending/loading progress skeleton in the verdict rail (`_PendingVerdict` renders the track for any verdict==null incl. RunPhase.error) — mixed signal after a failed run — enhancement — S
- [2026-07-06] (P4-recon shell-02) Brand presence inconsistent across surfaces — the "Quorum" wordmark lives inside TerminalBody only; Hub/Settings show none and the shell chrome carries no persistent mark — give the chrome one canonical brand anchor — enhancement — S
- [2026-07-06] (P4-recon shell-03) 36px title bar has no bottom divider; the active nav-tab accent underline sits flush on the title-bar/body seam (only a tonal step separates chrome from content) — add a 1px QC.border baseline — enhancement — S
- [2026-07-06] (P4-recon tok-02) No spacing/radius/type-scale tokens exist (QC/brand cover only color + font family) — every EdgeInsets/BorderRadius/fontSize is an inline magic number (BorderRadius.circular spans 4,8,10,11,12,20,99); against the stated token-first direction — enhancement (architecture) — M
- [2026-07-06] (P4-recon tok-03) All-caps section "eyebrow" label drifts in color+tracking across surfaces (Hub/Settings textMid/0.8 vs Terminal textLo/1.5–2.0) — fold into a shared eyebrow token when tok-02 lands — enhancement — S
- [2026-07-06] (P4-recon a11y-02) SELL `_RatingPill` down-text is 4.60:1 on surface2 — passes AA-normal but by only 0.1; a defensive tone/opacity bump would harden it — enhancement — S
- [2026-07-06] (P4-recon a11y-03) Hub custom InkWell controls (_MiniButton/_FilterChip/cast-list toggle) are not wrapped in `Focusable`, so they get Flutter's default focus overlay not the branded accent ring the rest of the app paints (still keyboard-operable — consistency, not an operability failure) — enhancement — S
- [2026-07-06] (P4-recon a11y-04) Hub edit fields (history filter, launch ticker, watchlist add) carry only `hintText`, no `labelText`/`Semantics(label:)` — an empty/focused field can read as unlabeled to a screen reader — enhancement — S
- [2026-07-06] (P4-recon a11y-05) Hub run-row merges two tappable controls (star watch-toggle + open-review InkWell) under one Semantics(button, container) node — ambiguous traversal; make the open action an explicit inner node — enhancement — S
