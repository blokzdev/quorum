# Goldens are now committed: S4 bundles Inter + JetBrains Mono and the test harness loads those
# bundled .ttf (see test/flutter_test_config.dart), so renders are deterministic across machines/CI.
# Regenerate after intentional UI changes with `flutter test --update-goldens` and review the diff.
