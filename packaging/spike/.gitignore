# P2.0 spike build artifacts — never commit (regenerated by PyInstaller).
build*/
dist*/
*.pyc
__pycache__/
# Re-track the .spec build recipes (the root .gitignore's `*.spec` rule would otherwise hide them).
!*.spec
