# P2.6b installer build artifacts — regenerated by build_installer.ps1 / PyInstaller / ISCC.
staging/
dist/
build/
output/
*.pfx
*.exe
# Re-track the build recipes (root .gitignore's *.spec would otherwise hide the spec).
!*.spec
